/**
 *
 * Beschreibung
 *
 * @version 1.0 vom 10.02.2022
 * @author 
 */

public class Sinus extends Funktion{
  double a; 
  double b; 
  double c; 
  double d;
    // Konstruktor 
  public Sinus(double a, double b, double c, double d) 
  { 
    this.a = a; 
    this.b = b; 
    this.c = c; 
    this.d = d; 
  }
    // Implementierung der abstrakten Funktion 
  public double fktwert(double x) 
  { 
    return a*Math.sin(c*(x-d))+b; 
  }
  // Anfang Attribute
  // Ende Attribute
  
  // Anfang Methoden
  // Ende Methoden
} // end of Sinus

